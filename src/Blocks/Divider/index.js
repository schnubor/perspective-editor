import component from './EditComponent.svelte';

export const initialSettings = {
    spacing: 'medium',
}

export default {
    id: 'divider',
    name: 'Divider',
    component,
    initialSettings,
}
